version: '2'
services:
  flask:
    restart: always
    build: flask
    expose:
      - "8001"
    volumes:
      - .:/opt/www
  nginx:
    restart: always
    build: nginx
    ports:
      - "8000:8000"
    volumes:
      - .:/opt/www
    links:
      - flask:flask
